Build Instructions
==================

1. Compile the source code using `g++` or visual studio.
2. Move the compiled binary to the `/commands` directory.

If you install it using paxon, 
.exe will automatically be placed in the `commands` directory.
